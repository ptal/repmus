package projects.music.editors;

public class SequenceRhythmic  {
	
/*	public RTree tree;
	public List<Chord> chords;
	
	
	public void nextChords (int n, Chord defchord) {
		if (chords.size() == 0){
			chords = new ArrayList<Chord>();
			chords.add(defchord);
		}
		else if (chords.size() > 1) {
			if (n >= chords.size()) {
				chords = new ArrayList<Chord>();
				chords.add(defchord);
			}
			else chords = chords.subList(n-1, chords.size() -1);
		}
	}
	*/

}
